<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Poslogin Demo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-blue-500 text-white flex justify-center items-center gap-2 p-2">
      <%= link_to "Users", users_path, class: 'hover:underline' %>
      <div>|</div>
      <%= link_to "Tasks", tasks_path, class: 'hover:underline' %>
    </nav>

    <main class="max-w-screen-lg container mx-auto mt-10 px-5">
      <%= yield %>
    </main>
  </body>
</html>
